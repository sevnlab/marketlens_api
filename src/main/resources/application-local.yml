# =====================================================
# 로컬 개발 환경 설정 (application-local.yml)
# Redis/Kafka/Oracle 없이 실행 가능, H2 인메모리 DB 사용
#
# 실행 방법 (IntelliJ):
#   Run Configuration -> Active profiles -> "local" 입력
# =====================================================

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

  datasource:
    url: jdbc:h2:mem:marketlens;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop

address:
  service:
    url: https://business.juso.go.kr/addrlink/addrLinkApi.do
    confirm-key: U01TX0FVVEgyMDI2MDQxMTIyNDUwNTExNzkxMTA=